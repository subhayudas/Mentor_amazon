/**
 * Mentor-facing Cal.com sync panel: webhook URL, secret, last delivery and
 * setup steps (design §3.6, F08). Phase 0 stub that renders nothing; Track B
 * implements it (B11).
 */
export function CalSyncPanel(_props: { mentorId: string; calLink?: string | null }): JSX.Element | null {
  return null;
}
